export type Point = {
  x: number;
  y: number;
};

export type Shape = Array<Point>;

export type Lines = {
  capLine: number;
  lowercaseLine: number;
  baseLine: number;
  descenderLine: number;
};
